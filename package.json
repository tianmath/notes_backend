{
  "name": "notes_backend",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "Mathieu",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no freaking test specified!\" && exit 1",
    "build:ui": "rm -rf dist && cd ../notes && npm run build && cp -r dist ../notes_backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.9.3"
  }
}
